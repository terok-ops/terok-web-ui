name: Release

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build production bundle
        run: npm run build

      - name: Prune dev dependencies
        run: npm prune --omit=dev

      - name: Create release artifact
        run: tar -czf terok-web-ui-dist.tar.gz dist node_modules package.json package-lock.json

      - name: Publish latest release asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build
          files: terok-web-ui-dist.tar.gz
          prerelease: true
